# Implementation status

Updated: **2026-09-19**. This is the implementation record for the [plan](cli-agent-plan.md). Task scope and acceptance remain in that plan; partial progress does not pass a delivery gate.

| Area                                         | Status                                                         | Evidence / next work                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| -------------------------------------------- | -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Existing UI localization                     | Complete                                                       | English/Chinese controls and errors, preference persistence, unit tests, and desktop/browser verification. Commit `d5b22f3`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Initial product scope                        | Recorded                                                       | OpenCode → Pi → DeepSeek Harness; English plan and research. Commit `3464c87`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| V1 / V3 discovery                            | Partial                                                        | Reproducible isolated CLI probe and [installed-version report](engine-probe-2026-09-18.md). No model calls or cross-platform acceptance                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| V5 Pi RPC lifecycle                          | Complete for macOS local fixture                               | Pi 0.85.1 passes prompt/event correlation, streaming/tools, cancellation, native restoration, extension dialogs, and project-trust checks. [Contract and evidence](pi-rpc.md); external provider acceptance remains open; desktop sessions pass a separate local fixture                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| C1 Pi configuration                          | Implemented; macOS fixture verified                            | Captured provider/Prompt/Skill mappings, secret environment references, separate project trust/context options, native model/Skill readback, and checked writable native state. [Evidence](pi-configuration.md); [runtime/factory wiring](pi-runtime.md) is verified; external provider acceptance remains open                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| C2 / C3 Pi runtime and desktop               | Implemented; macOS fixture verified                            | Bounded transport, normalized events, typed interactions, deadlines, cancellation, native restore, and desktop factory/UI wiring pass tests. [Runtime and desktop evidence](pi-runtime.md); selected [Pi extensions](pi-plugin-activation.md) now pass separate lifecycle checks; MCP and universal tool approval remain unsupported                                                                                                                                                                                                                                                                                                                                                                                                                          |
| V4 DSH route decision                        | Local ACP route verified                                       | Installed SDK lacks cancel/resume. Both selected ACP provider routes pass local tools, approvals, cancellation, patch precedence/restart, and native restoration. [Evidence and limits](dsh-acp.md); full DSH adapter/desktop acceptance remains open                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D1 DSH configuration                         | Implemented; macOS fixture verified                            | Pinned captured composition, isolated controls, separate provider routes, complete/prefix/suffix prompts, directory Skills, stdio MCP, tool approval/denial, and bilingual instruction placement pass tests. [Evidence and limits](dsh-configuration.md); [D2/D3 runtime and factory wiring](dsh-runtime.md) passes local fixtures; external provider acceptance remains open                                                                                                                                                                                                                                                                                                                                                                                 |
| D2 / D3 DSH runtime and desktop              | Implemented; macOS fixture verified                            | Separate CLI/ACP version checks, native model/session verification, committed messages/tools, correlated approvals, cancellation, durable history, and exact native restore pass both runtime routes and the bilingual desktop fixture. [Evidence](dsh-runtime.md); experimental limits remain explicit                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| D1 / D3 DSH selected plugins                 | Module activation implemented; macOS fixtures verified         | [Native module activation](dsh-plugin-activation.md), bounded bilingual JSON options, pinned framework/source checks, separate fibers per binding, native boot/current-session verification, and resume pass both provider routes and Electron. General bundle patch import and complete dependency provenance remain open                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| V2 provider calls                            | Pending input                                                  | Local Chat Completions and [Anthropic Messages](anthropic-provider-acceptance.md) fixtures pass; intended external endpoint/model and local key reference are still needed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| A1 / A8 shared schemas                       | Partial                                                        | Typed identities, drafts, secret references, optional sampling, and capability evidence are active in desktop and browser v2 persistence. Adapter-specific launch validation remains open                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| A4 MCP definitions                           | Partial                                                        | Bilingual definitions are active. OpenCode maps stdio and remote definitions; a real stdio fixture verifies cwd, argv, secret environment, permissions, and tool results. DSH stdio mapping is also verified. Local Streamable HTTP fixtures pass for OpenCode and both DSH routes. OpenCode records native status; DSH records initialization only. OAuth, external services and Pi MCP extensions remain unverified                                                                                                                                                                                                                                                                                                                                         |
| A11 configuration UI                         | Implemented foundation                                         | Shared libraries, three-engine native settings, credential-reference controls, pinned/latest bindings, directory imports, draft diagnostics, and planned resolved previews are active. Bilingual [engine compatibility](engine-capabilities.md) now blocks known adapter constraints before startup; native-source provenance and broader runtime capability verification remain open                                                                                                                                                                                                                                                                                                                                                                         |
| A8 capability dimensions                     | Partial; static and session capability descriptors implemented | [Engine compatibility](engine-capabilities.md) separates mechanism, runtime verification and availability, binds descriptors to profile/model-route identity, and blocks known adapter constraints in the UI and main process. [Attachment reports](session-capabilities.md) now aggregate eighteen scopes with native declarations separate from verified use and historical availability removed; broader native verification coverage remains open                                                                                                                                                                                                                                                                                                         |
| A10 session contract                         | Partial                                                        | Typed commands/events, lifecycle projection, correlated interactions, cancellation/resume boundaries, bounded cursor replay, and a durable journal with restart/torn-write recovery are tested. [Contract notes](session-contract.md); durable command receipts, journal-first coordination, scoped replies, cancellation escalation, and direct shutdown/recovery are implemented. Sender-checked IPC, desktop factory wiring, and bounded renderer reattachment are implemented                                                                                                                                                                                                                                                                             |
| A3 / A6 / A9 asset and migration foundations | Implemented foundation                                         | Versioned prompt/Skill editing, bundle/native-plugin separation, and v1 conversion are active. [Selected OpenCode, Pi and DSH plugin inspection](native-plugin-inspection.md) adds read-only entry/package metadata, version adoption, declared engine-range comparison, and digests; supported OpenCode ESM bindings, [Pi extensions](pi-plugin-activation.md), and [DSH modules](dsh-plugin-activation.md) now have separate startup/resume checks; complete dependency coverage remains open. Real Electron tests verify exact-byte backup, atomic migration, retained bindings, append-only edits, and restart                                                                                                                                            |
| A5 Skill directory assets                    | Partial                                                        | Desktop imports capture regular files, frontmatter, scripts, references, digests, and executable flags with bounded reads and atomic publication. Reimport retains old revisions; corrupt captures are preserved. [Boundaries](skill-capture.md); complete copies into run inputs are tested; OpenCode, Pi, and DSH directory Skill invocation and a combined [shared-asset update scenario](shared-asset-acceptance.md) pass local desktop fixtures; [Pi/DSH/OpenCode native source matching](skill-source-verification.md) now reject observed same-name substitutions; complete native collision/provenance reporting remains open                                                                                                                         |
| A7 credential service                        | Implemented; macOS verified                                    | Encrypted vault, revision-safe replacement/deletion, metadata-only IPC, bilingual Settings UI, and real Electron encryption/restart tests. Linux/Windows secure backends remain unverified                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| A12 shared resolution                        | Partial                                                        | Direct-over-bundle resolution, pinned/latest revisions, native-plugin ownership, and draft diagnostics are tested and visible in the UI. Per-session native observations and pending changes are now visible; full field-level native provenance and capability reporting remain open                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Native configuration import                  | Implemented subset; macOS fixture verified                     | [Selected-file import](native-configuration-import.md) adds bilingual previews, shared resources and disabled Agent drafts, encrypted exact-source retention, vault references, immutable provenance, optimistic conflicts, and interrupted-import recovery. An imported profile completes a native local-provider turn. The [Pi file-group importer](pi-native-configuration-import.md) is also implemented, as is [DSH selected YAML import](dsh-native-configuration-import.md); full native precedence, referenced resources, and complete runtime override provenance remain open                                                                                                                                                                        |
| B8 ACP infrastructure                        | Partial                                                        | Official SDK, bounded v1 framing, deadlines, ordered updates, and permission cancellation are tested. Application attachments initialize both ACP engines and clean their owned process groups; a local OpenCode protocol fixture covers streaming, tools, approvals, cancellation, and native resume. Durable OpenCode coordination passes the native fixture; desktop integration passes a separate Electron fixture. [Notes](acp-client.md)                                                                                                                                                                                                                                                                                                                |
| B4 process supervision                       | Partial; macOS verified                                        | Shell-free launch, explicit environment, bounded/redacted stderr, process-group cleanup after leader exit, forced termination, and ACP lifetime ownership are implemented. The coordinator awaits cleanup and preserves interrupted native identity. Electron quit/final-window hooks await cleanup. Windows supervision and Linux acceptance remain open. [Boundaries](process-lifecycle.md)                                                                                                                                                                                                                                                                                                                                                                 |
| B2 run input snapshots                       | Implemented foundation                                         | Atomic publication, resolved revision capture, independent Prompt/Skill copies, secret references, input integrity, isolated state, and observed-source reuse checks are tested. [Boundaries](run-inputs.md); OpenCode, Pi, and DSH native resume reuse verified snapshots and retained history. Explicit [closed-conversation deletion](session-retention.md) now coordinates shared references and recoverable cleanup; explicit [unused-data review and cleanup](unused-run-data.md) now covers recognized stages and verified unassociated captures, with durable intent and retry                                                                                                                                                                        |
| B3 OpenCode configuration                    | Partial; macOS fixture verified                                | Versioned provider/Prompt/Skill/MCP mapping, native-source observations, and in-memory credential injection are implemented. [Native evidence and limits](opencode-configuration.md); version/config readback and selected session model/agent gating are implemented. External provider acceptance, complete effective-policy checks, broader plugin coverage, and complete application reporting remain open. Explicit version checks and desktop launch are connected                                                                                                                                                                                                                                                                                      |
| B3 OpenCode plugin lifecycle                 | ESM activation implemented; macOS fixture verified             | [Installed plugin contract](opencode-plugin-contract.md) verifies legacy/V1 hooks, options, alias identity, soft failures, pure mode, and repeated initialization during startup/resume. [Production activation](opencode-plugin-activation.md) now gates Ready on source checks and current-instance initializer/config receipts; broader module/dependency coverage remains open; Pi and DSH have separate [Pi](pi-plugin-activation.md) and [DSH](dsh-plugin-activation.md) activation evidence.                                                                                                                                                                                                                                                           |
| B6 configuration and update reports          | Partial; reports and edit previews implemented                 | Bilingual reports show captured values, scoped native readback, unknown application, immutable asset versions/sources, saved-versus-captured changes, and durable attachment observations. [Evidence and limits](configuration-report.md). Bilingual [edit previews](library-impact.md) show related profiles, retained sessions, and pending versions. [Credential revision reports](credential-rotation.md) now compare durable attachment inputs with current vault metadata. [Configuration failure diagnostics](configuration-failures.md) identify failed checks and known field groups without native values; source inventories and diagnostic receipts retain their separate capture/attempt ownership. Full native override provenance remains open |
| B9 desktop session UI                        | Implemented; macOS fixture verified                            | OpenCode, Pi, and DSH saved-profile launch, per-launch directory selection, version checking, messages/tools, cancel/close, renderer replay, and restart/native resume work in English and Chinese. Directory overrides preserve saved defaults and captured resume identity. Native permissions are verified for OpenCode and DSH. [Workflow and evidence](desktop-sessions.md). Full stored-history browsing and JSONL export now pass the bilingual desktop fixture; explicit closed-conversation deletion and pending cleanup controls are implemented                                                                                                                                                                                                    |
| B0 / B7 / C4 / D4 gates                      | Not passed                                                     | All three runtime/desktop integrations pass local fixtures, including native resume; external provider acceptance, full native provenance, complete credential/data-boundary auditing, and broader plugin/bundle coverage remain open                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

The active application reads/writes schema v2 through a single store. Existing v1 files migrate with exact-byte backups; browser preview retains its old v1 key. All nine configuration collections have compatible English/Chinese editors. Native CLI installations used for probes are not automatically registered as user profiles. Saving an installation path does not execute a CLI. The explicit desktop Check installation action probes OpenCode, Pi, or DSH; only their pinned supported versions enable ACP or Pi RPC launch.

Latest verification: native capability reports in all three full desktop session fixtures, the three-engine credential-rotation Electron fixture (15 primary requests), both DSH four-file provider routes, the Pi five-file and OpenCode selected-Prompt three-file native-import Electron fixtures, the combined three-engine shared-asset Electron fixture, 781 unit tests (two workers), nine opt-in DSH tests across five probe files, the selected-plugin DSH Electron fixture, two opt-in installed-CLI ACP initialization/cleanup checks, an installed OpenCode runtime probe against a local protocol fixture (including two cancellation paths, native resume with retained history, permission persistence failure, override rejection, coordinator command replay, journal/event parity, and restart/resume), ESLint, TypeScript, production build, and changed-file formatting passed. Real Electron verification covers exact-byte v1 migration, all three engine drafts, Pi trust/context and DSH instruction placement, shared connections/models, prompt and Skill revisions, directory capture/reimport, pinned/latest bindings, bundles, bilingual diagnostics, deletion cleanup, restart, and OS-encrypted credential creation/replacement/deletion. The native folder chooser is substituted in the smoke test; the actual import IPC, capture service, filesystem, and editors run unchanged. The configuration smoke uses synthetic credentials without executing agents. A separate [desktop session smoke](desktop-sessions.md) runs installed OpenCode, Pi, and DSH against local synthetic providers, verifying explicit installation probing, profile-to-session capture, streamed/tool output, OpenCode permission replies and both cancellation paths, Pi streaming cancellation, renderer reload without resubmission, old/new shared-Prompt snapshots, app quit/restart, native resume, confirmed close, bilingual controls, escaped untrusted text, and journal key redaction. These fixtures do not call an external model provider.

The opt-in installed Pi probe additionally passes the [local RPC lifecycle fixture](pi-rpc.md), including Unicode streaming, tool context after native restoration, provider errors after acceptance, extension command handling without a model run, and unchanged unrelated trust decisions. The [saved-profile configuration fixture](pi-configuration.md) also passes actual directory Skill/reference reads, old/new prompt inputs, literal/secret headers, native state verification, and execution/trust/context choices. The [Pi runtime and desktop fixtures](pi-runtime.md) now pass normalized text/tool output, split-key redaction, durable command replay, streaming cancellation, post-acceptance provider errors, immutable snapshot reuse, renderer reload, and native restoration. English/Chinese desktop controls enable Pi 0.85.1 profiles with compatible execution policies. Universal tool approval remains unavailable.

The installed DSH ACP lifecycle also passes separately through its pi-ai gateway component and native DeepSeek component, both against local Chat Completions fixtures. [Results](dsh-acp.md) establish the selected route's feasibility and its committed-output, usage, and replay limits. All three engines now have early local lifecycle evidence; this does not replace external provider or complete desktop acceptance.

The [DSH configuration fixture](dsh-configuration.md) now passes both provider routes through the production profile generator and launcher. It verifies complete replacement with literal braces, persona prefix/suffix, preserved project rules, captured Skill/reference reads, stdio MCP argv/cwd/credential isolation, shared-Prompt revisions, and enforced ask/deny policies. The native composition readback and source/control integrity checks are active. The [DSH runtime and desktop fixtures](dsh-runtime.md) now verify both provider components through the production factory/coordinator and the generic route through Electron. Version/model checks, committed messages, tools, approvals, cancellation, journal redaction, and native resume pass. ACP context occupancy remains separate from unknown billing usage; the desktop labels DSH experimental.

Per-session [configuration reports](configuration-report.md) now persist bounded native check identifiers with the journaled Ready acknowledgment. Legacy sessions remain unverified until new evidence is recorded; interrupted sessions show historical observations. Reports compare resolved asset versions and executable inputs with the current library, honor fixed/direct bindings, and omit sensitive configuration without resolving credentials. The extended desktop fixtures cover English/Chinese reports, pending Prompt revisions, and evidence ownership before and after native resume. Bilingual [library-edit impact previews](library-impact.md) now cover affected profiles and retained conversations, including pinned/overridden bindings, draft diagnostics, removed profiles, unreadable captures, and bounded capture pagination. Credential revisions now have separate durable attachment observations and a metadata-only comparison. Complete native provenance remains open.

[Engine compatibility](engine-capabilities.md) now appears in Agent resolved previews and the session launcher. Shared adapter constraints block known unsupported settings before native-source inspection or capture; incompatible drafts remain editable. The desktop smoke explicitly awaits installation, turn and session IPC results instead of using asynchronous Playwright predicates, and checks both bilingual preflight diagnostics and the subsequent supported lifecycle.

The bilingual launch-directory workflow is now implemented and verified for all three installed CLIs through local desktop fixtures. New-session selection accepts a typed absolute path or a native folder chooser, validates host directories before native inspection, and resolves the preflight preview with that directory. Selection does not save the profile; cancellation preserves it, and existing conversations keep their captured directory. The extended fixture checks distinct relative-file results across two projects, Unicode/spaces, unchanged defaults, and native resume after restart. See the [workflow and per-engine records](desktop-sessions.md).

Bilingual [session history](session-history.md) now reads bounded pages beyond the live renderer cap and exports a fixed event prefix through the native Save dialog. Historical requests have no action controls; stored redaction and native output limits are preserved. All three desktop fixtures pass native read-only history plus a separate synthetic long-history scenario, exact JSONL event comparison, export cancellation, and reload without model resubmission. No external-provider or platform gate is promoted by these results.

[Pi extension activation](pi-plugin-activation.md) now passes installed CLI and Electron fixtures: explicit multi-entry packages, awaited factories, native imports/tools/system hooks, current-session activation receipts, extension commands/handled input without model calls, cancellable dialogs, and native resume. Source coverage remains partial; mixed-resource packages, generic options, MCP extension acceptance, and full dependency provenance remain open. DSH uses separate native module/configuration and lifecycle checks.

The [installed DSH plugin contract](dsh-plugin-contract.md) now verifies CommonJS objects, ESM functions/classes/generators, native configuration validation, separate fibers for repeated module bindings, service dependencies, custom tools/system sections, disposal, and native resume. A controlled delayed plugin proves that ACP session creation can precede complete native startup. The production [DSH activation path](dsh-plugin-activation.md) now waits for `appReady`, checks fresh process/row/fiber/session evidence before Ready and around turns, and obtains new evidence on resume. Bounded per-instance JSON options, package resolution, repeated module bindings, and bilingual editing/reports pass both native provider routes and the Electron fixture. General bundle patch import, complete dependency provenance, arbitrary hook behavior, external-provider acceptance, and other platforms remain open.

The [combined shared-asset acceptance fixture](shared-asset-acceptance.md) uses one workspace, Prompt, directory Skill, connection, and model profile for OpenCode, Pi, and DSH. It verifies eighteen native turns across six conversations: initial v1, edits during three active turns, subsequent old/new inputs, and both versions after app restart/native resume. Native tools read the actual captured entry and reference files; unique call IDs exclude stale transcript evidence. English/Chinese impact and version reports, unchanged Agent bindings, immutable manifests, and independence from edited/deleted source files pass. The endpoint is a local synthetic provider; external-provider and remaining delivery gates stay open.

The [OpenCode native configuration importer](native-configuration-import.md) now passes a real bilingual Electron fixture and a native 1.18.16 turn using imported endpoint/model/key/header/system-prompt settings. Preview does not execute the initially nonexistent CLI or read unselected references. Known literal credentials and exact source bytes are OS-encrypted; unknown fields retain diagnostic pointers and immutable provenance. Source changes, workspace conflicts, failed publication, lost acknowledgments, and restart recovery are covered. This is selected-file import, not complete native precedence or runtime override provenance. The Pi and DSH file-group importers now also pass local fixtures.

The [Pi 0.85.1 importer](pi-native-configuration-import.md) captures explicitly selected model/auth/settings JSON and replacement/append Markdown files in one encrypted transaction. Per-file integrity, stored-auth priority, inert secret templates, model-specific routes, defaults, and unsupported-policy diagnostics are covered. Its bilingual Electron fixture passes exact-source retention, secondary-file conflict rejection, restart/idempotent retry, and an imported native turn with sampling and literal/environment headers. The existing OpenCode import/native-call fixture also passes. Full native precedence, OAuth/compatibility mappings, broader native ingestion, and the delivery gates remain open.

The [DSH 0.1.5-rc.2 importer](dsh-native-configuration-import.md) now passes bilingual Electron fixtures for generic Pi-AI and native DeepSeek routes. It captures selected composition/patch/settings/credential YAML across folders, distinguishes whole-config patch replacement from recursive settings overrides, preserves inert expressions, and records explicit stored-key copies with ambient-precedence diagnostics. Per-file archive integrity, preview invalidation, rollback, restart and imported native calls pass. Full native discovery, grouped/component ingestion, complete override provenance and delivery gates remain open.

The [credential-rotation desktop fixture](credential-rotation.md) now verifies one shared vault reference across all three installed engines and 15 native model requests. Active processes retain the prior key; new sessions and native resumes use its replacement. Deletion blocks a later resume while preserving historical revision evidence. Reports in both languages omit key values, reference names, and opaque storage tokens; manifests remain unchanged. Unit coverage includes resolution/write races, legacy entries, same-ID recreation, encryption rewrapping, and failed Ready persistence. Environment values remain uncomparable, and external-provider, native DeepSeek route rotation, broader native-plugin/MCP, and cross-platform acceptance remain open.

[Native capability reports](session-capabilities.md) now aggregate eighteen scoped capabilities from recorded session checks, bounded ACP/Pi connection/restoration receipts, and launch-secret observations. Mechanism, verification and availability remain independent; advertisements alone cannot verify successful use. Identity covers the captured digest, adapter, installation, route, process and native conversation. All three full desktop fixtures pass English/Chinese tables, plugin checks, historical availability after restart, fresh native-restore evidence, and their existing lifecycle/history regressions. Unknown provider, full Prompt application, MCP connectivity and enforcement scopes remain explicit; complete A8 and delivery gates are still open.

[Configuration failure diagnostics](configuration-failures.md) now pass safe schema, field-classification, persistence, and retry tests. The desktop fixtures exercise OpenCode source drift and an actual native Prompt override, plus Pi/DSH control-file rejection and restoration. Detailed native values and winning-source attribution are not inferred. The prior [OpenCode resource inventory](native-resource-sources.md) captures Markdown membership without parsing or importing it.

[Session retention](session-retention.md) now provides confirmed deletion of closed conversations, durable cleanup receipts, cursor and attachment checks, shared-capture protection, and startup/manual retry. The macOS desktop fixtures pass for all three engines, including unchanged project/shared-library/export data and no model calls during deletion; Pi and DSH also exercise the retry button. The full suite passes 696 tests across 49 files, with lint, TypeScript, and production build checks. Broader native data-location coverage, external providers, and cross-platform gates remain open. The later unused-data workflow below covers recognized stages and verified unassociated captures.

[Captured Skill source checks](skill-source-verification.md) now compare Pi RPC names/source paths and OpenCode native preflight selections against immutable adapter mappings. Same-name substitutions fail without model requests in installed-release fixtures. Reports distinguish RPC, separate-process preflight, and unknown evidence, preserve legacy/historical meaning, and show mapped native entries separately from archival copies. All three bilingual desktop fixtures pass; the subsequent DSH observer described below adds its own native receipt. Full native collision/precedence attribution, external-provider acceptance, and platform gates remain open. The later OpenCode ACP observer below replaces the source preflight for new captures.

The [DSH Skill-source observer](dsh-skill-sources.md) now verifies selected filesystem entries in the attached ACP Agent’s scope at startup/resume and turn boundaries. Native tests keep the global catalog intact while substituting a same-name scoped Skill, and verify rejection before any new provider request on both provider routes. The bilingual Electron report and lifecycle fixture pass. Older DSH captures remain usable with source evidence unknown. This closes the DSH selected-source gap; complete collision/provenance inventories, external-provider acceptance, and delivery gates remain open.

The [OpenCode ACP Skill-source observer](opencode-skill-sources.md) now checks selected entries through the actual owned native server at startup/resume and turn boundaries. Ephemeral loopback authentication stays outside captured inputs; no observer plugin is needed. ACP-only same-name overrides fail before provider calls on new and resumed sessions. Legacy separate-process receipts retain their scope. Native authentication/shutdown checks and the bilingual Electron lifecycle fixture verify the change. Complete collision/precedence attribution, external providers, other platforms, and delivery gates remain open.

[Unused run data](unused-run-data.md) now has a bilingual review and confirmed removal workflow. Creation and cleanup share catalog/store queues; fresh journal, pending-deletion and active-attachment references protect retained captures. Durable receipts support partial cleanup/restart recovery and prevent replayed creation from regenerating removed inputs. Unknown or corrupt entries are preserved for inspection. The three installed-engine desktop fixtures cover confirmation cancellation, project symlink boundaries, pending retry, unchanged shared data and zero cleanup model calls. Complete external/native provenance, broader data-boundary auditing, external-provider acceptance and other-platform gates remain open.

The [OpenCode selected Prompt importer](native-configuration-import.md#explicit-prompt-source-assignment) now captures explicitly chosen replacement and appended instruction files across folders, with source-to-field provenance, ordered shared bindings, literal nested macros, and encrypted exact-byte bundles. Legacy single-file archives remain compatible. The [bilingual Electron result](probes/2026-09-19-opencode-prompt-import-desktop.json) verifies chooser cancellation, secondary-source conflicts, restart/idempotent retry and the imported native turn. This is explicit file assignment; full native discovery, glob composition, precedence and delivery gates remain open.

[OpenCode ACP instance configuration checks](opencode-instance-configuration.md) now compare captured requirements with the owned server on startup, resume and around turns, even without selected Skills. The listener and bounded session-identity window are shared with Skill observations. Bilingual reports preserve distinct current-instance and legacy preflight scopes. The installed fixture rejects ACP-only startup/resume overrides and active configuration drift before another provider call; the full OpenCode desktop fixture verifies new diagnostics, restart, native restoration and lifecycle behavior. All five OpenCode native fixtures and 781 unit tests pass. Complete winning-source attribution and native precedence remain open.

[HTTP MCP acceptance](http-mcp-acceptance.md) now passes the production OpenCode and both DSH runtime routes against local Streamable HTTP services. Bearer, secret-header and unauthenticated configurations, literal ordinary headers, JSON/SSE responses, session/protocol headers, actual tool results/errors, secret redaction, failed startup/omitted tools and native resume after service recovery are verified. OpenCode continues without failed MCP tools; DSH rejects startup. The later OpenCode observer records connection status and the DSH observer records initialization only. Dedicated per-server failure diagnostics, OAuth, external MCP services, Pi extension acceptance and delivery gates remain open.

[OpenCode MCP status observations](opencode-mcp-status.md) now sample the authenticated ACP server at attachment, persist selected-only status enums with run identity, and display English/Chinese per-service rows. Configuration mapping remains separate; all selected connections are required for a passing scoped capability. Missing or unreadable status is unknown, explicit native failure is visible without blocking the conversation, and successful native resume refreshes historical evidence. The later DSH observer records initialization separately. OAuth login, continuous connectivity, external services and delivery gates remain open.

[DSH MCP initialization evidence](dsh-mcp-startup.md) now waits for native startup, verifies selected Loader components and their configuration/session identity, and records bilingual per-service initialization results. The connectivity capability remains unverified because native registrations can survive service failure. Fresh receipts on resume, legacy captures, failed attachment and source-specific schema boundaries retain explicit scope. Full native and desktop results are recorded in that document; external services, live connection status, Pi MCP extensions and delivery gates remain open.

[OpenCode instruction reference observations](opencode-instruction-sources.md) now record local literal/glob matches, including empty selectors, and recheck content/membership on capture, startup/resume and active turn boundaries. Native calls verify relative, home, hidden and ancestor rules and reject changed references before model calls; reports preserve historical metadata in both languages. Remote/dynamic references remain explicitly unobserved. Complete precedence, external-service acceptance and delivery gates remain open.

[Encrypted credential masking history](credential-redaction-history.md) fixes a reproduced old-key leak after credential rotation and native resume. New desktop captures retain OS-encrypted masking values before launching children, including OpenCode observer credentials; authentication uses only freshly resolved references. Missing or corrupt required history blocks attachment, and capture cleanup removes retained copies. Reports and credential settings explain the policy in both languages. Legacy coverage, native plaintext transcripts, external services, broader plugin/MCP behavior and full X3 acceptance remain explicit boundaries.

The [Anthropic Messages acceptance fixture](anthropic-provider-acceptance.md) now passes all three installed engines with both root and versioned shared endpoints. The adapter mapping fixes duplicated or missing `/v1` paths while retaining proxy prefixes; native imports preserve request meaning and Pi readback retains compatibility with older captures. Actual headers, Prompt markers, tool results, cancellation, restoration, failure responses and output masking pass six local cases. Bilingual Electron endpoint guidance, 875 unit tests, lint and builds pass. External-provider, other-platform and full delivery gates remain open.

The [Responses acceptance fixture](responses-provider-acceptance.md) adds all three pinned engines on a versioned proxy base. Selected Bearer/header credentials, Prompt markers, fragmented function calls with real native file results, cancellation, native restoration, HTTP failures and output masking pass. A Chat Completions-only gateway's HTTP 404 fails without protocol fallback. All nine combined provider cases pass sequentially; no production mappings change. Malformed successful streams, external services, other platforms and full delivery gates remain open.

[OpenCode conflict source evidence](opencode-override-sources.md) now identifies captured literal JSON/JSONC declarations matching rejected native values, for both separate preflight and owned ACP-instance checks. Indexed metadata survives journal restart and resolves to historical paths in the bilingual report; matching files remain ambiguous candidates, and dynamic/unsupported sources remain unknown. The installed Electron fixture verifies an actual JSONC Prompt conflict, unchanged inputs, no model calls on rejection, and the same evidence after deleting the source and restarting. All 887 unit tests, lint and builds pass. Complete precedence, external-provider acceptance and delivery gates remain open.

[Application crash cleanup](process-crash-recovery.md) fixes a reproduced native process leak after host `SIGKILL`. A guardian retains group ownership independently of Electron, preserves native PID/exit semantics and forwards bounded protocol streams; the main process provides fallback cleanup after guardian loss. Subprocess checks cover both crash directions and resistant descendants. OpenCode 1.18.16, Pi 0.85.1 and DSH 0.1.5-rc.2 each pass the macOS Electron fixture with an active provider turn, four owned groups removed, closed local streams, interrupted history without resubmission, and explicit restoration of the same native session and snapshot. Other platforms, packaged builds, external-service acceptance and full X3/X4 and delivery gates remain open.

[Installed plugin inspection](native-plugin-inspection.md) now exposes the existing Pi and DSH file resolvers in the library editor alongside OpenCode. Pi reports every declared entry and both engine peer ranges; DSH preserves ancestor-package lookup and separates the saved CLI version from unknown framework dependency versions. Allowlisted names, package paths, versions and digests support explicit version adoption without executing code or changing native IDs/sources. The three-engine bilingual Electron fixture verifies readonly behavior, package restrictions, errors, stale-result clearing and saved references after restart. Persisted runtime inspection identities are unchanged. Complete dependency provenance, native bundle import and delivery gates remain open.

[Selected plugin dependency observations](plugin-dependency-sources.md) now detect helper-only changes across OpenCode, Pi, and both DSH provider routes. Static relative-module traversal records hashes, package scopes and absence, with unresolved categories and explicit partial coverage. English/Chinese reports preserve historical metadata without source bodies; legacy capture digests remain unchanged. Validation includes 922 unit tests, lint/typecheck/build, four installed-CLI route cases, and the three full Electron lifecycle fixtures. Complete dependency closure, external-provider acceptance, and delivery gates remain open.

[OpenCode plugin options](opencode-plugin-options.md) now provide bilingual JSON editing and native tuple delivery to legacy/V1 initializers. Captures preserve literal macro keys/values and old options across saved edits and native resume; new captures use edited options. Engine mismatch controls require an explicit clear before changing option semantics. Validation includes 923 unit tests, lint/typecheck/build, the installed OpenCode activation fixture, the full OpenCode Electron lifecycle fixture and the three-engine configuration editor fixture. Plugin secret-reference injection, complete dependency provenance and delivery gates remain open.
